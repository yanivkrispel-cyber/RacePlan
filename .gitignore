node_modules/
.DS_Store
*.log

# Claude Code local/tool artifacts (per-machine, not project config)
.claude/settings.local.json
.thumbnail
.image-slots.state.json
uploads/

# Apps Script client bundle — generated by `npm run build`.
# `npm run push` / `npm run redeploy` always rebuild first, so these don't
# need to be committed and would just be noisy 250KB diffs.
apps-script/AppJs.gs
apps-script/index.html
